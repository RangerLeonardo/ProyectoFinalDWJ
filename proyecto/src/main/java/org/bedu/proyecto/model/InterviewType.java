package org.bedu.proyecto.model;

public class InterviewType extends Discipline {

    public InterviewType(String name, String slug, String description) {
        super(name, slug, description);
    }
}
